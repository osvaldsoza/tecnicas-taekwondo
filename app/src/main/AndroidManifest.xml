<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.osvaldsoza.tecnicas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/tae"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/tae"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tecnicas">
        <activity
            android:name=".FaixaBrancaActivity"
            android:exported="false"
            android:label="@string/faixa_branca"
            android:theme="@style/Theme.Faixa.Branca" />
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:label="@string/graduacoes"
            android:name=".MainActivity"
            android:exported="true" />
    </application>

</manifest>